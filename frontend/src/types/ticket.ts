// Re-export shared types and schemas
export type { Ticket, TicketResponse, CreateTicketDTO, UpdateTicketDTO } from '@shared/types'

export {
  TicketSchema,
  CreateTicketSchema,
  UpdateTicketSchema,
  TicketTypeEnum,
  TicketStatusEnum,
  TicketPriorityEnum,
} from '@shared/types'
