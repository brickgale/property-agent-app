// Re-export shared types and schemas
export type { Address, AddressResponse, CreateAddressDTO, UpdateAddressDTO } from '@shared/types'

export {
  AddressSchema,
  CreateAddressSchema,
  UpdateAddressSchema,
  AddressableTypeEnum,
} from '@shared/types'
